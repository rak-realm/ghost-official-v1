{
  "name": "GHOST-OFFICIAL-V1",
  "description": "An advanced, futuristic WhatsApp bot by RAK for the RAK Realm ecosystem.",
  "keywords": [
    "whatsapp",
    "bot",
    "automation",
    "rak-realm"
  ],
  "stack": "container",
  "env": {
    "BOT_NAME": {
      "description": "Name of your bot",
      "required": false,
      "value": "GHOST-OFFICIAL-V1"
    },
    "OWNER_NAME": {
      "description": "Bot owner name",
      "required": false,
      "value": "RAK"
    },
    "RAK_SESSION": {
      "description": "Your WhatsApp session data. Keep this secure.",
      "required": false
    },
    "PREFIX": {
      "description": "Bot command prefix. Use / for the new RAK style.",
      "required": false,
      "value": "/"
    },
    "LANGUAGE": {
      "description": "Bot language. Options: EN, UR, HI, AR.",
      "required": false,
      "value": "EN"
    },
    "SUDO_USERS": {
      "description": "Admin users (comma-separated numbers with country code). Example: 923123456789,919876543210",
      "required": false
    },
    "MOD_USERS": {
      "description": "Moderator users (comma-separated numbers with country code).",
      "required": false
    },
    "ALLOWED_JIDS": {
      "description": "JIDs allowed to use the bot (comma-separated). Leave empty for public.",
      "required": false
    },
    "BLOCKED_JIDS": {
      "description": "JIDs blocked from using the bot (comma-separated).",
      "required": false
    },
    "LOG_LEVEL": {
      "description": "Logging level. Options: error, warn, info, debug.",
      "required": false,
      "value": "info"
    },
    "AUTO_UPDATE": {
      "description": "Enable auto-update from GitHub. (true/false)",
      "required": false,
      "value": "true"
    },
    "ANTI_BAN": {
      "description": "Enable anti-ban protection system. (true/false)",
      "required": false,
      "value": "true"
    },
    "VC_MODE": {
      "description": "Default state for Voice Command Mode. (on/off)",
      "required": false,
      "value": "off"
    },
    "SEND_READ": {
      "description": "Send read receipts. (true/false)",
      "required": false,
      "value": "false"
    },
    "NO_ONLINE": {
      "description": "Hide online status. (true/false)",
      "required": false,
      "value": "true"
    },
    "WARN_COUNT": {
      "description": "Number of warnings before action (default: 3)",
      "required": false,
      "value": "3"
    },
    "MAX_REQUESTS_PER_MINUTE": {
      "description": "Rate limiting (default: 30)",
      "required": false,
      "value": "30"
    },
    "HEROKU_APP_NAME": {
      "description": "Required for Heroku. Your Heroku app name.",
      "required": true
    },
    "HEROKU_API_KEY": {
      "description": "Required for Heroku. Your Heroku API key.",
      "required": true
    },
    "OPENAI_KEY": {
      "description": "OpenAI API key for AI features (optional)",
      "required": false
    },
    "BRAINSHOP_KEY": {
      "description": "Brainshop API key for AI chat (optional)",
      "required": false
    },
    "REMOVEBG_KEY": {
      "description": "Remove.bg API key for image processing (optional)",
      "required": false
    }
  },
  "addons": [
    {
      "plan": "heroku-postgresql:hobby-dev"
    }
  ],
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ],
  "formation": {
    "web": {
      "quantity": 0,
      "size": "free"
    },
    "worker": {
      "quantity": 1,
      "size": "free"
    }
  }
}
